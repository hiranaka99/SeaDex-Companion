# SeaDex Companion - Python (server) dependencies
#
# These cover the Flask backend (app.py), the diagnostic script (diag.py)
# and the original CLI (SeaDEX Compare.py). All other imports are stdlib.
#
# NOTE: The React + Vite + TypeScript frontend (frontend/) is NOT a Python
# dependency - it is built with `npm install && npm run build` into static/,
# which Flask then serves. Do not add React/Vite packages here.

flask==3.1.3
requests==2.34.2
waitress==3.0.2